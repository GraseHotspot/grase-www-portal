For build
# yarn encore production
Bump VERSION file numbers
Make data-tables default paging configurable per user

alter table batch
	add constraint batch_pk
		primary key (batchID, UserName);

Find a real JS translation engine that does ICU message format (https://formatjs.io/github/)

autocreatepassword needs to be present and randomised if not already present (Setting Sanity)
DB upgrade to cleanup radgroupreply/check: select * from radgroupreply WHERE GroupName NOT IN (SELECT id from groups);
Verify group limits (groups vs radgroupcheck)
Settings Sanity radreply coovachilli values

Redo templates

Work out default network interfaces to set CoovaChilli on (first run wizard?)
